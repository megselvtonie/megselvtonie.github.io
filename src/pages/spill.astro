---
import BaseLayout from "../layouts/BaseLayout.astro";
import DateTag from "../components/DateTag.astro";
import GameCard from "../components/GameCard.astro";
import { getCollection, type CollectionEntry } from "astro:content";


type Game = CollectionEntry<"games">;
const games: Game[] = await getCollection("games");
export const breadcrumb = "Spilloteket";

const pageTitle = "Spilloteket → mitt";
---

<BaseLayout pageTitle={pageTitle}>
  <h1>Spilloteket med det rare i <DateTag datetime="2025-09-26T14:06" /></h1>
  <p class="subhead">Under utvikling ass </p>
  <p class="ingress">Her skal det stå en ingress, men jeg får ikke til å designe den som jeg vil!!</p>
  <h2>Jeg tester noen funksjoner her</h2>
  <h3>Dette er en underoverskrift</h3>
  <p> Sliter som faen med å justere skriftstørrelsene på tekstene "#$%"$%&</p>

  <section class="grid">
    {games.map((game) => <GameCard game={game} />)}
  </section>
</BaseLayout>

<style>
.grid {
  display: grid;
  gap: 1rem;
  grid-template-columns: 1fr; /* alltid én kolonne */
  margin-top: 1.5rem;
}

/* Desktop: én kolonne */
@media (min-width: 1024px) {
  .grid {
    grid-template-columns: 1fr;
  }
}
</style>
